---
format:   
  dashboard:
    theme: yeti
    scrolling: false
    expandable: false
editor_options: 
  chunk_output_type: console
---

<style>
.plot-d6a7b5-swatches-wrap {
  justify-content: center;
}
</style>

```{r create-topojson}
#| echo: false     # hides the code
#| results: "hide" # hides printed output
#| message: false  # hdides messages (e.g. from package loading)
#| warning: false  # hides warnings

library(sf)
library(magrittr)

end_year = 2025

dir.create("assets",
           recursive = TRUE,
           showWarnings = FALSE)

## Keyed on the FSA county, matching both the archive and the boundary geometry the
## map draws. The geometry is keyed on FSA codes, so keying the data on FIPS was a
## mismatch. Fourteen FSA counties here carry a code that differs from the FIPS code
## of the county they fall in, and under FIPS keying seven of them painted a
## neighbouring FSA county's polygon — Southeast Nye's determination coloured
## Northwest Nye, West Pottawattamie's coloured East Pottawattamie, and Houlton's
## and Fort Kent's coloured Aroostook — while the other seven did not render at all.
## Silent misattribution, not a visible gap.
##
## No reduction is needed at this grain. FSA administers seven Census counties as
## two or three offices and determines eligibility for each separately; keyed on the
## FSA county each is its own polygon, so the halves draw side by side rather than
## one of them being dropped.
readr::read_csv("fsa-lfp-eligibility.csv") %>%
  dplyr::transmute(
    id = paste0(`FSA State Code`, `FSA County Code`),
    year = `Program Year`,
    type = `Pasture Type`,
    disaster =`Disaster Type`,
    disaster_start_date = `Date of Qualifying Drought`,
    qualifier = `Note (FOIA 2025-FSA-04690-F Bocinsky)`,
    payment_type = ifelse(is.na(`Payment Factor`), "Eligible", paste(`Payment Factor`, "Month"))) %>%
  dplyr::arrange(id, type, dplyr::desc(year), disaster) %>%
  dplyr::filter(year <= end_year) %>%
  readr::write_csv(
    file.path(
      "assets",
      "fsa-lfp-eligibility-simple.csv"
    ), 
    quote = "all")

```


```{ojs inputs}
//| content: card-toolbar

// Create interactive inputs

viewof disaster = Inputs.radio(
["Drought", "Fire"],
{
label: "Disaster",
value: "Drought"
}
)

viewof year = Inputs.range(
d3.extent(data, d => d.year),
{
step: 1, 
label: "Year",
value: d3.max(data, d => d.year)
}
)

viewof type = {
  const options = disaster === "Fire"
    ? ["Rangeland"]
    : [...new Set(data.map(d => d.type))].sort();
  return Inputs.select(options, { label: "Type", value: "Native Pasture" });
}
```

```{ojs read-data}
//| output: false

// Import libraries
scaleSequential = (await import("https://cdn.jsdelivr.net/npm/d3@7/+esm")).scaleSequential

// a format for dates
formatDate = d3.utcFormat("%b %d, %Y")  // e.g., "Apr 18, 2025"

// counties

// FSA county geometry, from the fsa-counties-dd22 archive. Its polygons are keyed
// on the 5-character FSA county code, which is what the data extract now uses —
// previously the extract was keyed on FIPS, so the 150 FSA counties whose code
// differs from their county's FIPS code never joined and never rendered.
//
// dd22 rather than dd17, and rather than switching by program year as the normal
// grazing period dashboard does. Measured across all 18 program years, dd22
// resolves at least as many FSA codes as dd17 in every one and one more in six of
// them, and it alone defines 16079 (Shoshone, ID). Only five records of 51,348 —
// 31045 and 31165 in 2008, 51087 in 2010 — name themselves the way dd17 draws
// them, which does not earn a second fetch and a year-dependent branch.
us = await d3.json("https://data.sustainable-fsa.com/fsa-counties-dd22/fsa-counties-dd22.topojson");
counties = topojson.feature(us, us.objects.counties)

// states, but just the boundaries between them
states = topojson.mesh(us, us.objects.states, (a, b) => a !== b)

// normal grazing period data, simplified
data = FileAttachment("assets/fsa-lfp-eligibility-simple.csv").csv({typed: true})

// Load color palettes
color =
new Map([
["1 Month", "#FFFF54"], 
["2 Month", "#F3AE3D"],
["3 Month", "#6D4E16"],
["4 Month", "#EA3323"],
["5 Month", "#7316A2"],
["Eligible", "#EA3323"]
])

variable = "payment_type"

```

```{ojs filter-join-data}
//| output: false

// Filter CSV data to current selections
filtered = data.filter(d => d.year === year && d.type === type && d.disaster === disaster)

// Create a lookup table keyed by `id`
valueById = Object.fromEntries(filtered.map(d => [String(d.id).padStart(5, "0"), d[variable]]))

// Merge values into counties.features
countiesFiltered = {
  // Build lookup from filtered CSV: id → full row
  const rowById = Object.fromEntries(
        filtered.map(d => [String(d.id).padStart(5, "0"), d])
  );

  // Rebuild counties with joined properties
  return {
    type: "FeatureCollection",
    features: counties.features.map(f => {
      const id = String(f.id);
      const row = rowById[id];

      return {
        ...f,
        value: row ? row[variable] : null,
        properties: {
          id: f.id,
          ...f.properties,
          ...(row ?? {}),
          value: row ? row[variable] : null  // explicitly add value for coloring
        }
      };
    })
  };
}

```

```{ojs map-size}
//| output: false

// Size the map to its card. A ResizeObserver notifies as soon as the card
// receives its real dimensions, so the FIRST render is correctly sized —
// the stdlib `width` is only re-evaluated on window resize, which left the
// initial paint at the wrong scale until the viewport changed.
mapSize = Generators.observe((notify) => {
  const body = document.querySelector(".card .card-body");
  const ro = new ResizeObserver((entries) => {
    const r = entries[0].contentRect;
    if (r.width > 0 && r.height > 0) notify({width: r.width, height: r.height});
  });
  ro.observe(body);
  return () => ro.disconnect();
})
```

```{ojs}
//| label: map
//| title: FSA Livestock Forage Disaster Program Eligibility

Plot.plot({
width: mapSize.width,
height: Math.max(300, mapSize.height - 70),  // leave room for the legend
projection: {
    type: "albers",
    domain: counties
  },
grid: true,
color: (disaster === "Drought" ? 
{
    domain: ["1 Month", "2 Month", "3 Month", "4 Month", "5 Month"], 
    range: ["#FFFF54", "#F3AE3D", "#6D4E16", "#EA3323", "#7316A2"],
    unknown: "#ccc",
    legend: true
  } : 
  {
    domain: ["Eligible"], 
    range: ["#EA3323"],
    unknown: "#ccc",
    legend: true
  }),
marks: [
Plot.geo(countiesFiltered, {
      fill: "value"
    }),

Plot.dot(
  countiesFiltered.features.map(f => {
    const [x, y] = d3.geoCentroid(f);
    return {
      ...f.properties,
      x,
      y
    };
  }),
  {
    x: "x",
    y: "y",
    r: 3,
    fill: "transparent",
    stroke: "none",
    tip: true,
    title: d => `${d.county}, ${d.state}
FSA county code: ${String(d.id).padStart(5, "0")}
Program Year: ${year}
Crop Type: ${type}
Disaster: ${d.disaster ? d.disaster : "None"}
Drought Qualifier: ${d.qualifier ? d.qualifier : "None"}
Start Date: ${d.disaster_start_date ? formatDate(d.disaster_start_date) : "None"}
`
  }
),

Plot.geo(states, {stroke: "white"}),
]
})

```

<!-- ## Row -->

<!-- ```{ojs} -->
<!-- Inputs.table(filtered) -->
<!-- ``` -->